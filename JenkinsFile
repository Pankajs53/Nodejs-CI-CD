pipeline {
  agent {
    docker { image 'node:16-alpine' }
  }
  stages {
    stage('Test') {
      steps {
        sh 'node --version'
      }
    }
    stage('Build And Test') {
      steps {
        sh 'ls -ltr'
        sh 'npm install'
      }
    }
  }
}
